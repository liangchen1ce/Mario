function [P] = gamePort(audioName)
% This script is from LabRosa

% Read an audio waveform
[d,sr] = wavread(audioName);

[x,FS,NBITS]=wavread(audioName); % ? WAV ???????
x1=x(:,1); % ??? 1 ??
x2=x(:,2); % ??? 2 ??
wavwrite(x1,FS,NBITS,'XPquit1.WAV'); % ?? 1 ????
wavwrite(x2,FS,NBITS,'XPquit2.WAV'); % ?? 2 ????
%??????????? 0 %????????? 0
x12=x1+x2; % ??????????????
x12m = max(max(x12),abs(min(x12))); % ????
y12=x12./x12m; % ?????
newName = 'MonoTrack.wav';
wavwrite(y12,FS,NBITS,newName); % ????????
[d,sr] = wavread(newName);

% Calculate the chroma matrix.  Use a long FFT to discriminate
% spectral lines as well as possible (2048 is the default value)
cfftlen=2048;
C = chromagram_IF(d,sr,cfftlen);

P = 20*log10(C+eps);
